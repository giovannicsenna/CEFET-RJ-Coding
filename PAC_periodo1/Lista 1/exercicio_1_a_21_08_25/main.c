#include <stdio.h>

int criaNumero(int x, int y) {
    int soma = x + y;
}

int main() {
    int x = 2500;
    int y = 10;
    int resultado = criaNumero(x, y);

    printf("O resultado da soma é %d", resultado );
}